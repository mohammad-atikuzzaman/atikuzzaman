import React, { FC } from 'react';
import ProjectCard from './ProjectCard';

const Projects:FC = () => {
    return (
        <div>
            <ProjectCard/>
        </div>
    );
};

export default Projects;